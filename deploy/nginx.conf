server {
    listen 80;
    server_name TU_DOMINIO_O_IP;

    # Redirigir HTTP → HTTPS (descomenta cuando tengas certificado SSL)
    # return 301 https://$host$request_uri;

    location = /favicon.ico { access_log off; log_not_found off; }

    location /static/ {
        alias /opt/binabot/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/binabot/gunicorn.sock;
        proxy_read_timeout 120;
        proxy_connect_timeout 120;
    }
}

# Descomenta y configura con certbot cuando tengas SSL:
# server {
#     listen 443 ssl;
#     server_name TU_DOMINIO;
#     ssl_certificate /etc/letsencrypt/live/TU_DOMINIO/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/TU_DOMINIO/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#     ... (mismos bloques location de arriba)
# }
